
<div class="p-user-index">

  <%= paginate @users %>

  <ul class="p-user-index-list">
    <% @users.each do |user| %>
      <li>
        <%= gravatar_for user, size: 50 %>
        <%= link_to user.name, user %>
      </li>
    <% end %>
  </ul>

  <%= paginate @users %>

</div>